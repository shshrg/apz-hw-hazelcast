### Task2. Hazelcast
The description of what each class is responsible for can be found in my report (on Moodle).
Any of the .java files can be ran with Maven:
```
mvn clean compile
mvn exec:java "-Dexec.mainClass=com.example.[file name]" -e
```
